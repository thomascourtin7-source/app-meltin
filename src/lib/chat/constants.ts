/** Prénom chat (messages + liaison `user_name` pour les push). */
export const CHAT_USERNAME_STORAGE_KEY = "meltin_chat_username";

/** Émis par `ChatPageShell` à chaque resize/scroll du VisualViewport (clavier). */
export const CHAT_VIEWPORT_RESIZE_EVENT = "chat-viewport-resize";
